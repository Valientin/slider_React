import React from 'react';
import './header.scss';

const Header = () => {
    return(
        <header>
            <div className="container">
                <h3 className="header-title">My slider</h3>
            </div>
        </header>
    )
}

export default Header;